#include "HASH_PESSOAS.h"
#include <stdio.h>
#include <stdlib.h>
//
// Created by luiz-p on 26/05/2026.
//
PESSOA* cria_pessoa() {
    PESSOA *novo_pessoa = (PESSOA*)malloc(sizeof(PESSOA));
    if (!novo_pessoa) {exit(1);}
    novo
}